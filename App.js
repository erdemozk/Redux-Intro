import React from 'react';
import {
  SafeAreaView,
  StyleSheet,
  ScrollView,
  View,
  Text,
  StatusBar,
} from 'react-native';

const App = () => {
  return (
    <SafeAreaView>
      <Text>
        Selam
      </Text>
    </SafeAreaView>
  );
};

const styles = StyleSheet.create({

});

export default App;
