import React from 'react';
import {
  SafeAreaView,
  StyleSheet,
  ScrollView,
  View,
  Text,
  StatusBar,
} from 'react-native';
import { Provider } from 'react-redux';

const App = () => {
  return (
    <SafeAreaView>
      <Text>
        Selam
      </Text>
    </SafeAreaView>
  );
};

const styles = StyleSheet.create({

});

export default App;
